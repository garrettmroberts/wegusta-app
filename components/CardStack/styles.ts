import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    marginBottom: 440
  },
  cardPlacement: {
    position: 'absolute',
    left: 0,
    right: 0,
    bottom: 0,
    top: 0,
    alignItems: 'center'
  }
});

export default styles;
